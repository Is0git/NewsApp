package com.is0git.multicategorylayout.category_controller

import com.is0git.multicategorylayout.category_data.Category


internal class DefaultCategoryController<T> : CategoryController<T> {
    override suspend fun resetCategories(categories: MutableMap<String, Category<T>>) {

    }

    override suspend fun updateCategories(categories: MutableMap<String, Category<T>>) {

    }

    override suspend fun addCategory(category: Category<T>) {

    }

}