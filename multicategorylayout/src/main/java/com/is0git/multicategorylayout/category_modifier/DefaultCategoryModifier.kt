package com.is0git.multicategorylayout.category_modifier

import com.is0git.multicategorylayout.category_data.Category

class DefaultCategoryModifier : CategoryModifier {
    override fun modifyCategories(categories: List<*>) {

    }

    override fun modifyCategory(categories: Category<*>) {

    }
}